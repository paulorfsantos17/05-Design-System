{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {
    "@storybook/addon-docs": "^8.0.8"
  },
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build"
  },
  "devDependencies": {
    "turbo": "^1.13.2"
  }
}